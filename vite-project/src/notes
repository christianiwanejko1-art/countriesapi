set up github repo
nav bar, search bar - make sure these work correctly - add filter
display the countries - all of them on main page (population, region, capital)
https://restcountries.com/v3.1/all?fields=name,flags,population,region

display one country, then display all

country card -> add flag to top (new div), add content text to bottom (bottom div)
display all on home page

(onclick) of card open and show about the card - re render home on click of back button
take the card name add api call it

mainContent - nav, back, flag/container/info, empty space
to render the main information on the page it should take the value and use for api call (single with corresponding data)

add new margin left and right to main content
add image on left side, data on the right side -> Country name, native name, population, region, sub region, capital, top level domain, currencies, languages

re-render nav when clicked on the flag
